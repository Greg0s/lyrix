name = "lyrix-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# Local dev default only. In production, set the real secret with:
#   wrangler secret put STATE_SECRET --config worker/wrangler.toml
[vars]
STATE_SECRET = "dev-secret-change-me"
